# How to run QMIND FSA?

`npm install`
This will install all the required packages for this React application.

`npm run start`
This will run the application.

`npm run build`
Builds the production build.
